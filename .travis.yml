language: elixir
elixir:
  - 1.3.4
  - 1.4.5
  - 1.5.1
otp_release:
  - 19.3
script:
  - mix test && mix credo --strict && mix dialyzer --halt-exit-status
notifications:
  recipients:
    - igasgeek@me.com
